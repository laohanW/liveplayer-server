import models from '../models'
export let recomContent = async (type) => {
  return {
    resCode: 0,
    msg: null,
    response: null
  };
}
export let childRecomContent = async (category) => {
  return {
    resCode: 0,
    msg: null,
    response: null
  };
}
